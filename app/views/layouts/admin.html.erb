<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "管理画面 - Reception Rails" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-100">
    <% if current_admin_user %>
      <!-- ナビゲーションバー -->
      <nav class="bg-white shadow-md">
        <div class="container mx-auto px-4">
          <div class="flex justify-between items-center h-16">
            <div class="flex items-center space-x-8">
              <%= link_to admin_root_path, class: "flex items-center" do %>
                <span class="text-xl font-bold text-gray-900">受付システム管理</span>
              <% end %>
              
              <!-- メニュー -->
              <div class="flex items-center space-x-1">
                <%= link_to "従業員", 
                    admin_employees_path, 
                    class: "px-4 py-2 rounded-lg transition-colors #{current_page?(admin_employees_path) || controller_name == 'employees' ? 'bg-blue-100 text-blue-700 font-semibold' : 'text-gray-700 hover:bg-gray-100'}" %>
                <%= link_to "部署", 
                    admin_departments_path, 
                    class: "px-4 py-2 rounded-lg transition-colors #{current_page?(admin_departments_path) || controller_name == 'departments' ? 'bg-blue-100 text-blue-700 font-semibold' : 'text-gray-700 hover:bg-gray-100'}" %>
                <%= link_to "SmartHR同期", 
                    admin_smarthr_sync_path, 
                    class: "px-4 py-2 rounded-lg transition-colors #{current_page?(admin_smarthr_sync_path) ? 'bg-blue-100 text-blue-700 font-semibold' : 'text-gray-700 hover:bg-gray-100'}" %>
              </div>
            </div>
            <div class="flex items-center space-x-4">
              <span class="text-gray-700"><%= current_admin_user.name %></span>
              <%= button_to "ログアウト", 
                  admin_logout_path, 
                  method: :delete,
                  class: "px-4 py-2 bg-red-600 text-white rounded hover:bg-red-700 transition-colors" %>
            </div>
          </div>
        </div>
      </nav>
    <% end %>

    <% if flash[:notice] %>
      <div class="container mx-auto px-4 mt-4">
        <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded relative" role="alert">
          <%= flash[:notice] %>
        </div>
      </div>
    <% end %>
    <% if flash[:alert] %>
      <div class="container mx-auto px-4 mt-4">
        <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative" role="alert">
          <%= flash[:alert] %>
        </div>
      </div>
    <% end %>
    
    <main class="container mx-auto px-4 py-8">
      <%= yield %>
    </main>
  </body>
</html>

